diff --git a/src/components/admin/AdminLayout.jsx b/src/components/admin/AdminLayout.jsx
index eaa5426..06b1687 100644
--- a/src/components/admin/AdminLayout.jsx
+++ b/src/components/admin/AdminLayout.jsx
@@ -27,11 +27,6 @@ export function AdminHeader({ title, showBack = false }) {
       <header className="app-header">
         <Toranam />
         <div className="app-header-row">
-          {showBack && (
-            <button className="back-btn" onClick={() => navigate(-1)} aria-label="Go back">
-              <ChevronLeft size={22} />
-            </button>
-          )}
           <Link to="/admin" className="brand" aria-label="Admin dashboard">
             <img src="/icon-192.png" alt="" className="brand-logo" />
             <span className="brand-name">{t('app_name')}</span>
@@ -44,7 +39,7 @@ export function AdminHeader({ title, showBack = false }) {
             >
               <span className={lang === 'en' ? 'active' : ''}>EN</span>
               <span className="sep">|</span>
-              <span className={lang === 'te' ? 'active' : ''}>తె</span>
+              <span className={lang === 'te' ? 'active' : ''}>తెలుగు</span>
             </button>
             <button
               className="lang-toggle"
@@ -59,7 +54,16 @@ export function AdminHeader({ title, showBack = false }) {
           </div>
         </div>
       </header>
-      {title && <h1 className="page-title-bar">{title}</h1>}
+      {title && (
+        <div className={`page-title-bar${showBack ? ' with-back' : ''}`}>
+          {showBack && (
+            <button className="page-title-back" onClick={() => navigate(-1)} aria-label="Go back">
+              <ChevronLeft size={20} />
+            </button>
+          )}
+          <h1 className="page-title-bar-text">{title}</h1>
+        </div>
+      )}
     </>
   );
 }
diff --git a/src/pages/Home.jsx b/src/pages/Home.jsx
index b85d80b..b29a92e 100644
--- a/src/pages/Home.jsx
+++ b/src/pages/Home.jsx
@@ -43,7 +43,7 @@ export default function Home() {
   );
 }
 
-function FestivalCountdown({ festival, t }) {
+function FestivalCountdown({ festival, t, lang }) {
   if (!festival.startDate || !festival.endDate) return null;
   const startOfDay = (d) => { const x = new Date(d); x.setHours(0, 0, 0, 0); return x; };
   const today = startOfDay(new Date());
@@ -54,7 +54,7 @@ function FestivalCountdown({ festival, t }) {
   if (today > end) {
     label = t('countdown_over');
   } else if (today >= start) {
-    label = today.getTime() === start.getTime() ? t('countdown_today') : t('countdown_live');
+    label = `${festival.year} — ${festival.name[lang]}`;
   } else {
     const days = Math.round((start - today) / 86400000);
     label = (days === 1 ? t('countdown_days_one') : t('countdown_days_other')).replace('{n}', days);
@@ -112,17 +112,11 @@ function HomeContent({ data, t, lang }) {
               <CalendarDays size={14} style={{ verticalAlign: -2, marginRight: 4 }} />
               {festival.dates[lang]}
             </div>
-            <FestivalCountdown festival={festival} t={t} />
+            <FestivalCountdown festival={festival} t={t} lang={lang} />
           </>
         ) : (
           <h1 style={{ fontSize: 'var(--fs-lg)' }}>{t('home_no_festival')}</h1>
         )}
-        <div className="modak-icon">
-          <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
-            <path d="M24 10c9 0 16 8 16 16 0 8-7 12-16 12S8 34 8 26c0-8 7-16 16-16Z" fill="#F6B93B" opacity="0.9" />
-            <path d="M24 10c2-4 5-6 8-6" stroke="#F6B93B" strokeWidth="2.5" strokeLinecap="round" />
-          </svg>
-        </div>
       </section>
 
       {nothingPublishedYet && (
diff --git a/src/styles/app.css b/src/styles/app.css
index 8e33576..428a951 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -83,18 +83,41 @@
   position: sticky;
   top: 0;
   z-index: 19;
-  margin: 0;
+  display: flex;
+  align-items: center;
+  gap: 8px;
   padding: var(--space-3) var(--space-4);
+  background: var(--color-surface);
+  border-bottom: 1px solid var(--color-border);
+  animation: titleBarIn 0.22s ease;
+}
+@keyframes titleBarIn {
+  from { opacity: 0; transform: translateY(-4px); }
+  to { opacity: 1; transform: translateY(0); }
+}
+.page-title-back {
+  display: flex;
+  align-items: center;
+  flex-shrink: 0;
+  color: var(--color-ink);
+  transition: transform 0.15s ease;
+}
+.page-title-back:active { transform: scale(0.88) translateX(-2px); }
+.page-title-bar-text {
+  flex: 1;
+  min-width: 0;
+  margin: 0;
   font-family: var(--font-display);
   font-size: var(--fs-md);
   color: var(--color-ink);
   text-align: center;
-  background: var(--color-surface);
-  border-bottom: 1px solid var(--color-border);
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
 }
+.page-title-bar.with-back .page-title-bar-text {
+  text-align: left;
+}
 
 .lang-toggle {
   display: inline-flex;
@@ -113,12 +136,21 @@
 .toranam {
   display: block;
   width: 100%;
-  height: 20px;
+  height: 30px;
   margin-top: -1px;
-  margin-bottom: -6px;
+  margin-bottom: -8px;
   line-height: 0;
 }
 .toranam svg { display: block; width: 100%; height: 100%; }
+.toranam svg path:not(:first-child) {
+  animation: toranamTwinkle 2.6s ease-in-out infinite;
+  transform-origin: center;
+}
+.toranam svg path:nth-child(odd) { animation-delay: 0.4s; }
+@keyframes toranamTwinkle {
+  0%, 100% { opacity: 1; }
+  50% { opacity: 0.55; }
+}
 
 /* ---------- Auth shell (login) — always centered, never the sidebar grid ---------- */
 .auth-shell {
@@ -224,8 +256,11 @@
   font-weight: 600;
   padding: 6px 2px;
   position: relative;
+  transition: color 0.2s ease, transform 0.15s ease;
 }
+.nav-item:active { transform: scale(0.92); }
 .nav-item.active { color: var(--color-vermillion); }
+.nav-item.active svg { animation: navPop 0.32s ease; }
 .nav-item.active::before {
   content: "";
   position: absolute;
@@ -235,6 +270,16 @@
   border-radius: var(--radius-pill);
   background: var(--color-marigold);
   box-shadow: 0 0 8px rgba(240,148,15,0.7);
+  animation: navIndicatorIn 0.22s ease;
+}
+@keyframes navPop {
+  0% { transform: scale(0.7); }
+  60% { transform: scale(1.15); }
+  100% { transform: scale(1); }
+}
+@keyframes navIndicatorIn {
+  from { opacity: 0; transform: scaleX(0.3); }
+  to { opacity: 1; transform: scaleX(1); }
 }
 .nav-item .nav-label {
   max-width: 62px;
@@ -390,17 +435,17 @@ button > .card:active {
 }
 .hero.hero-has-photo { cursor: zoom-in; }
 @keyframes pulseGlow {
-  0%, 100% { box-shadow: 0 0 0 0 rgba(246,185,59,0.35); }
-  50% { box-shadow: 0 0 0 6px rgba(246,185,59,0); }
+  0%, 100% { box-shadow: 0 0 0 0 rgba(194,43,31,0.55); }
+  50% { box-shadow: 0 0 0 6px rgba(194,43,31,0); }
 }
 @media (prefers-reduced-motion: reduce) {
   .hero-countdown { animation: none; }
-}
-.hero .modak-icon {
-  position: absolute;
-  right: var(--space-4);
-  bottom: var(--space-3);
-  opacity: 0.9;
+  .nav-item.active svg,
+  .nav-item.active::before,
+  .page-title-bar,
+  .toranam svg path {
+    animation: none;
+  }
 }
 
 /* ---------- Buttons ---------- */
@@ -906,7 +951,9 @@ button > .card:active {
   display: flex; align-items: center; justify-content: center;
   background: var(--color-surface-alt);
   color: var(--color-ink);
+  transition: transform 0.12s ease, background 0.15s ease;
 }
+.icon-btn:active { transform: scale(0.88); }
 .icon-btn.whatsapp { background: #DDF3E3; color: #1e8f47; }
 .icon-btn.call { background: #E4EFFB; color: #1f6fbf; }
 
