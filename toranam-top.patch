diff --git a/src/components/Header.jsx b/src/components/Header.jsx
index e436859..3dd96c8 100644
--- a/src/components/Header.jsx
+++ b/src/components/Header.jsx
@@ -10,6 +10,7 @@ export default function Header({ title, showBack = false, onBack }) {
   return (
     <>
       <header className="app-header">
+        <Toranam />
         <div className="app-header-row">
           {showBack && (
             <button className="back-btn" onClick={onBack || (() => navigate(-1))} aria-label="Go back">
@@ -30,7 +31,6 @@ export default function Header({ title, showBack = false, onBack }) {
             <span className={lang === 'te' ? 'active' : ''}>తెలుగు</span>
           </button>
         </div>
-        <Toranam />
       </header>
       {title && <h1 className="page-title-bar">{title}</h1>}
     </>
diff --git a/src/components/admin/AdminLayout.jsx b/src/components/admin/AdminLayout.jsx
index dc29d39..eaa5426 100644
--- a/src/components/admin/AdminLayout.jsx
+++ b/src/components/admin/AdminLayout.jsx
@@ -25,6 +25,7 @@ export function AdminHeader({ title, showBack = false }) {
   return (
     <>
       <header className="app-header">
+        <Toranam />
         <div className="app-header-row">
           {showBack && (
             <button className="back-btn" onClick={() => navigate(-1)} aria-label="Go back">
@@ -57,7 +58,6 @@ export function AdminHeader({ title, showBack = false }) {
             </button>
           </div>
         </div>
-        <Toranam />
       </header>
       {title && <h1 className="page-title-bar">{title}</h1>}
     </>
diff --git a/src/styles/app.css b/src/styles/app.css
index 4e7b801..068966b 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -113,8 +113,9 @@
 .toranam {
   display: block;
   width: 100%;
-  height: 14px;
+  height: 20px;
   margin-top: -1px;
+  margin-bottom: -6px;
   line-height: 0;
 }
 .toranam svg { display: block; width: 100%; height: 100%; }
