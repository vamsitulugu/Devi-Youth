diff --git a/src/i18n/en.js b/src/i18n/en.js
index 0850e06..dc8c302 100644
--- a/src/i18n/en.js
+++ b/src/i18n/en.js
@@ -63,6 +63,7 @@ export const en = {
   gallery_empty_sub: 'Photo albums will appear here once the committee uploads them.',
   gallery_album_empty: 'No photos in this album yet.',
   gallery_album_empty_sub: 'Photos will appear here once they are uploaded.',
+  gallery_no_photos_yet: 'No photos yet',
   gallery_all_albums: 'All Albums',
   gallery_photos_word: 'photos',
   home_latest_photos_empty: 'Photos will appear here once the committee starts uploading.',
diff --git a/src/i18n/te.js b/src/i18n/te.js
index 27e0865..71677a7 100644
--- a/src/i18n/te.js
+++ b/src/i18n/te.js
@@ -63,6 +63,7 @@ export const te = {
   gallery_empty_sub: 'కమిటీ అప్‌లోడ్ చేసిన తర్వాత ఫోటో ఆల్బమ్‌లు ఇక్కడ కనిపిస్తాయి.',
   gallery_album_empty: 'ఈ ఆల్బమ్‌లో ఇంకా ఫోటోలు లేవు.',
   gallery_album_empty_sub: 'అప్‌లోడ్ చేసిన తర్వాత ఫోటోలు ఇక్కడ కనిపిస్తాయి.',
+  gallery_no_photos_yet: 'ఫోటోలు లేవు',
   gallery_all_albums: 'అన్ని ఆల్బమ్‌లు',
   gallery_photos_word: 'ఫోటోలు',
   home_latest_photos_empty: 'కమిటీ అప్‌లోడ్ చేయడం మొదలుపెట్టిన తర్వాత ఫోటోలు ఇక్కడ కనిపిస్తాయి.',
diff --git a/src/pages/Contacts.jsx b/src/pages/Contacts.jsx
index 4e823cf..806d5e6 100644
--- a/src/pages/Contacts.jsx
+++ b/src/pages/Contacts.jsx
@@ -6,6 +6,14 @@ import Header from '../components/Header';
 import EmptyState from '../components/EmptyState';
 import { PageSkeleton, PageError } from '../components/LoadingStates';
 
+// Short emergency numbers (e.g. "100", "108") aren't real WhatsApp/mobile
+// numbers, so we only show the WhatsApp action for full 10-digit mobile
+// numbers (optionally with a country code like +91).
+function isWhatsAppCapable(phone) {
+  const digits = (phone || '').replace(/[^0-9]/g, '');
+  return digits.length === 10 || (digits.length === 12 && digits.startsWith('91'));
+}
+
 export default function Contacts() {
   const { t, lang } = useLanguage();
   const { data: contacts, loading, error, reload } = useAsyncData(getContacts, []);
@@ -32,15 +40,17 @@ export default function Contacts() {
                   <a className="icon-btn call" href={`tel:${c.phone}`} aria-label={t('call')}>
                     <Phone size={16} />
                   </a>
-                  <a
-                    className="icon-btn whatsapp"
-                    href={`https://wa.me/${c.phone.replace(/[^0-9]/g, '')}`}
-                    target="_blank"
-                    rel="noopener noreferrer"
-                    aria-label="WhatsApp"
-                  >
-                    <MessageCircle size={16} />
-                  </a>
+                  {isWhatsAppCapable(c.phone) && (
+                    <a
+                      className="icon-btn whatsapp"
+                      href={`https://wa.me/${c.phone.replace(/[^0-9]/g, '')}`}
+                      target="_blank"
+                      rel="noopener noreferrer"
+                      aria-label="WhatsApp"
+                    >
+                      <MessageCircle size={16} />
+                    </a>
+                  )}
                 </div>
               </div>
             ))}
diff --git a/src/pages/Gallery.jsx b/src/pages/Gallery.jsx
index 401f751..a1c3a20 100644
--- a/src/pages/Gallery.jsx
+++ b/src/pages/Gallery.jsx
@@ -63,7 +63,8 @@ export default function Gallery() {
                     <img src={a.cover} alt="" loading="lazy" decoding="async" />
                   ) : (
                     <div className="album-cover-empty">
-                      <ImageIcon size={28} strokeWidth={1.5} />
+                      <ImageIcon size={26} strokeWidth={1.5} />
+                      <span>{t('gallery_no_photos_yet')}</span>
                     </div>
                   )}
                   {a.count > 0 && (
diff --git a/src/styles/app.css b/src/styles/app.css
index 068966b..56457cc 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -639,9 +639,19 @@ button > .card:active {
   width: 100%;
   height: 100%;
   display: flex;
+  flex-direction: column;
   align-items: center;
   justify-content: center;
-  color: var(--color-border);
+  gap: 6px;
+  color: var(--color-vermillion);
+  background:
+    radial-gradient(circle, rgba(168,121,31,0.10) 1.5px, transparent 1.6px) 0 0 / 18px 18px,
+    var(--color-surface-alt);
+}
+.album-card .album-cover-empty span {
+  font-size: var(--fs-xs);
+  font-weight: 600;
+  color: var(--color-text-muted, #8a7a6a);
 }
 .album-card .album-cover-badge {
   position: absolute;
